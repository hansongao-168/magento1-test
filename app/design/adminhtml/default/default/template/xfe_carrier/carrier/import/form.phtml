<?php
/**
 * XFE Carrier - Upload form (rendered inside the import container).
 *
 * @var XFE_Carrier_Block_Adminhtml_Carrier_Import_Form $this
 */
$helper = Mage::helper('xfe_carrier');
?>
<form id="carrier_import_form" action="<?php echo $this->getPostUrl() ?>" method="post" enctype="multipart/form-data">
    <?php echo $this->getBlockHtml('formkey') ?>
    <div class="entry-edit">
        <div class="entry-edit-head">
            <h4 class="icon-head head-edit-form fieldset-legend"><?php echo $helper->__('CSV 文件') ?></h4>
        </div>
        <fieldset id="carrier_import_fieldset" class="fieldset">
            <div class="hor-scroll">
                <table class="form-list">
                    <tbody>
                        <tr>
                            <td class="label">
                                <label for="carrier_csv"><?php echo $helper->__('CSV 文件') ?> <span class="required">*</span></label>
                            </td>
                            <td class="value">
                                <input type="file" name="carrier_csv" id="carrier_csv" class="required-entry input-file" />
                            </td>
                        </tr>
                        <tr>
                            <td class="label">&nbsp;</td>
                            <td class="value">
                                <p class="note" style="max-width: 560px;">
                                    <?php echo $helper->__(
                                        '上传 .csv 文件。首行必须是表头,可包含 name / code / shipping_company_id。'
                                        . '同一文件内 code 不可重复;重复 code 视为同一承运商,后者覆盖前者。'
                                        . 'shipping_company_id 留空表示未指定;允许负数(线路公司占位 ID,如 -1001)。'
                                    ) ?>
                                </p>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </fieldset>
    </div>
    <div class="form-buttons" style="margin-top: 10px;">
        <button type="submit" class="scalable save">
            <span><span><span><?php echo $helper->__('上传并导入') ?></span></span></span>
        </button>
    </div>
</form>
<script type="text/javascript">
    var carrierImportForm = new varienForm('carrier_import_form');
</script>