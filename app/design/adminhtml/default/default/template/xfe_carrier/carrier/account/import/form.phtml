<?php
/**
 * XFE Carrier - 账号上传表单（渲染在账号导入容器内部）。
 *
 * @var XFE_Carrier_Block_Adminhtml_Carrier_Account_Import_Form $this
 */
$helper = Mage::helper('xfe_carrier');
?>
<form id="carrier_account_import_form" action="<?php echo $this->getPostUrl() ?>" method="post" enctype="multipart/form-data">
    <?php echo $this->getBlockHtml('formkey') ?>
    <div class="entry-edit">
        <div class="entry-edit-head">
            <h4 class="icon-head head-edit-form fieldset-legend"><?php echo $helper->__('CSV 文件') ?></h4>
        </div>
        <fieldset id="carrier_account_import_fieldset" class="fieldset">
            <div class="hor-scroll">
                <table class="form-list">
                    <tbody>
                        <tr>
                            <td class="label">
                                <label for="account_csv"><?php echo $helper->__('CSV 文件') ?> <span class="required">*</span></label>
                            </td>
                            <td class="value">
                                <input type="file" name="account_csv" id="account_csv" class="required-entry input-file" />
                            </td>
                        </tr>
                        <tr>
                            <td class="label">&nbsp;</td>
                            <td class="value">
                                <p class="note" style="max-width: 640px;">
                                    <?php echo $helper->__(
                                        '上传 .csv 文件。首行必须是表头,可包含 carrier_code / account_name / account_no / '
                                        . 'api_key / api_secret / username / password / endpoint_url / status / sort_order / note。'
                                        . 'carrier_code 与 account_name 为必填;account_no 可选(空值将存为空字符串)。'
                                        . '同一承运商下,account_no 非空时按 (account_name + account_no) 定位、'
                                        . '为空时按 account_name 定位,命中已存在账号则更新,否则新增。'
                                    ) ?>
                                </p>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </fieldset>
    </div>
    <div class="form-buttons" style="margin-top: 10px;">
        <button type="submit" class="scalable save">
            <span><span><span><?php echo $helper->__('上传并导入') ?></span></span></span>
        </button>
    </div>
</form>
<script type="text/javascript">
    var carrierAccountImportForm = new varienForm('carrier_account_import_form');
</script>
