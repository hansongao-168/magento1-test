<?php
/**
 * XFE Carrier - 规则上传表单（渲染在规则导入容器内部）。
 *
 * @var XFE_Carrier_Block_Adminhtml_Carrier_Rule_Import_Form $this
 */
$helper = Mage::helper('xfe_carrier');
?>
<form id="carrier_rule_import_form" action="<?php echo $this->getPostUrl() ?>" method="post" enctype="multipart/form-data">
    <?php echo $this->getBlockHtml('formkey') ?>
    <div class="entry-edit">
        <div class="entry-edit-head">
            <h4 class="icon-head head-edit-form fieldset-legend"><?php echo $helper->__('CSV 文件') ?></h4>
        </div>
        <fieldset id="carrier_rule_import_fieldset" class="fieldset">
            <div class="hor-scroll">
                <table class="form-list">
                    <tbody>
                        <tr>
                            <td class="label">
                                <label for="rule_csv"><?php echo $helper->__('CSV 文件') ?> <span class="required">*</span></label>
                            </td>
                            <td class="value">
                                <input type="file" name="rule_csv" id="rule_csv" class="required-entry input-file" />
                            </td>
                        </tr>
                        <tr>
                            <td class="label">&nbsp;</td>
                            <td class="value">
                                <p class="note" style="max-width: 640px;">
                                    <?php echo $helper->__(
                                        '上传 .csv 文件。首行必须是表头,可包含 carrier_code / module_code / name / description / '
                                        . 'status / is_cancel_on_failure / sort_order / priority / account_code / conditions_json。'
                                        . 'carrier_code 与 name 为必填;同一承运商下同名规则将被更新,否则新增。'
                                        . 'conditions_json 为条件树 JSON,格式见下载的模板。'
                                    ) ?>
                                </p>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </fieldset>
    </div>
    <div class="form-buttons" style="margin-top: 10px;">
        <button type="submit" class="scalable save">
            <span><span><span><?php echo $helper->__('上传并导入') ?></span></span></span>
        </button>
    </div>
</form>
<script type="text/javascript">
    var carrierRuleImportForm = new varienForm('carrier_rule_import_form');
</script>
